# Google Places API Configuration
GOOGLE_API_KEY=your_google_places_api_key_here

# Semicolon-delimited search queries
# Each query will be sent as a separate API request
TEXT_QUERY=dentists in Austin, TX; med spas in Austin, TX; orthodontists in Austin, TX

# Rating bounds for filtering businesses
# LOWER_BOUND: Minimum rating (e.g., 3 for 3.0 stars)
# UPPER_BOUND: Maximum user rating count (e.g., 150 for smaller businesses)
LOWER_BOUND=3
UPPER_BOUND=150
